<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('./partials/head.ejs')%>
</head>
<body>
  <nav id="partials">
    <!-- ============ HEADER ============ --> 
    <%- include('./partials/header.ejs') %>
  </nav>
  <div class="show-golden">
    <h1><%= product.name %></h1><br/>
    <img src="<%= product.img%>.jpg" width="200" height="200">
    <p><%= product.description %></p>
    <p><%= product.price %></p>
 
  <!-- Delete section -->
  <form action="/products/<%= product._id %>?_method=DELETE" method="POST">
    <input type="submit" value="Delete" />
  </form>
    
  <!-- Edit page -->
  <a href="/products/<%= product._id %>/edit">Edit Jewelry</a>
</div>
  <!-- ============ FOOTER ============ -->
  <%- include('./partials/footer.ejs') %>
</body>
</html>